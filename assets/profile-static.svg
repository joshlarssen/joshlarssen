<svg width="1200" height="940" viewBox="0 0 1200 940" fill="none" xmlns="http://www.w3.org/2000/svg">
          <defs>
            <linearGradient id="bg" x1="0" y1="0" x2="1200" y2="940" gradientUnits="userSpaceOnUse">
              <stop stop-color="#030703" />
              <stop offset="1" stop-color="#071109" />
            </linearGradient>
            <pattern id="scan" width="6" height="6" patternUnits="userSpaceOnUse">
              <rect width="6" height="3" fill="#0E200F" opacity="0.2" />
            </pattern>
            <style><![CDATA[
        :root {
  color-scheme: dark;
  --bg-start: #030603;
  --bg-end: #050b05;
  --panel: rgba(7, 19, 9, 0.94);
  --line: #2f9134;
  --line-soft: rgba(76, 180, 80, 0.22);
  --text: #9dff7b;
  --text-strong: #6dff55;
  --text-dim: #62c964;
  --text-faint: #2f6a33;
  --shadow: rgba(57, 255, 20, 0.18);
  --surface-glow: rgba(109, 255, 85, 0.08);
}

* {
  box-sizing: border-box;
}

html,
body {
  margin: 0;
  min-height: 100%;
  font-family: 'IBM Plex Mono', 'SFMono-Regular', Consolas, monospace;
  background:
    radial-gradient(circle at top, rgba(39, 120, 44, 0.15), transparent 32%),
    linear-gradient(180deg, var(--bg-start) 0%, var(--bg-end) 100%);
  color: var(--text);
}

body {
  position: relative;
  overflow-x: hidden;
}

.crt {
  position: fixed;
  inset: 0;
  pointer-events: none;
  background:
    linear-gradient(rgba(255, 255, 255, 0.03) 50%, rgba(0, 0, 0, 0.08) 50%),
    linear-gradient(90deg, rgba(255, 0, 0, 0.02), rgba(0, 255, 0, 0.02), rgba(0, 0, 255, 0.02));
  background-size: 100% 6px, 8px 100%;
  opacity: 0.15;
}

.shell {
  max-width: 1240px;
  margin: 0 auto;
  padding: 28px 18px 40px;
}

.topbar,
.screen,
.dock,
.footer {
  border: 1px solid var(--line);
  background: var(--panel);
  box-shadow: 0 0 30px var(--shadow), inset 0 0 0 1px rgba(67, 183, 66, 0.12);
}

.topbar {
  display: flex;
  gap: 18px;
  justify-content: space-between;
  align-items: center;
  padding: 18px 22px;
  border-radius: 18px 18px 0 0;
}

.brand {
  display: flex;
  gap: 14px;
  align-items: center;
}

.brand-led {
  width: 18px;
  height: 18px;
  border-radius: 50%;
  background: var(--text-strong);
  box-shadow: 0 0 18px rgba(109, 255, 85, 0.85);
}

.brand-kicker,
.eyebrow {
  margin: 0;
  color: var(--text-dim);
  text-transform: uppercase;
  letter-spacing: 0.18em;
  font-size: 0.72rem;
}

.brand-title,
.sidebar-title,
.panel-title {
  margin: 2px 0 0;
  color: var(--text-strong);
}

.brand-title {
  font-size: clamp(1.7rem, 3vw, 2rem);
  text-transform: lowercase;
}

.module-tabs {
  display: flex;
  flex-wrap: wrap;
  gap: 10px;
  justify-content: flex-end;
}

.tab-button,
.item-button,
.link-pill {
  border: 1px solid var(--line);
  background: rgba(7, 18, 8, 0.9);
  color: var(--text);
  font: inherit;
  cursor: pointer;
  transition: 140ms ease;
  text-decoration: none;
}

.tab-button {
  padding: 10px 14px;
  text-transform: uppercase;
  letter-spacing: 0.14em;
}

.tab-button:hover,
.tab-button:focus-visible,
.tab-button.is-active,
.item-button:hover,
.item-button:focus-visible,
.item-button.is-active,
.link-pill:hover,
.link-pill:focus-visible {
  background: var(--text-strong);
  color: #041006;
  box-shadow: 0 0 18px rgba(102, 255, 68, 0.26);
  outline: none;
}

.screen {
  display: grid;
  grid-template-columns: minmax(250px, 320px) minmax(0, 1fr);
  gap: 18px;
  padding: 18px;
  border-top: none;
}

.sidebar,
.panel-frame,
.dock {
  border: 1px solid var(--line-soft);
}

.sidebar {
  padding: 18px;
  background:
    linear-gradient(180deg, rgba(15, 31, 16, 0.76), rgba(5, 13, 6, 0.92)),
    linear-gradient(180deg, transparent, rgba(109, 255, 85, 0.04));
}

.item-list {
  display: grid;
  gap: 10px;
  margin-top: 18px;
}

.item-button {
  display: flex;
  justify-content: space-between;
  align-items: center;
  gap: 12px;
  width: 100%;
  padding: 14px;
}

.item-button__index {
  color: var(--text-faint);
}

.item-button.is-active .item-button__index {
  color: rgba(4, 16, 6, 0.75);
}

.panel-frame {
  min-height: 100%;
  padding: 22px;
  background:
    linear-gradient(180deg, rgba(8, 18, 8, 0.94), rgba(3, 8, 4, 0.97)),
    linear-gradient(90deg, transparent, rgba(109, 255, 85, 0.03), transparent);
}

.panel-meta {
  display: flex;
  gap: 18px;
  justify-content: space-between;
  align-items: flex-start;
}

.panel-title {
  font-size: clamp(2rem, 4vw, 3.5rem);
  line-height: 1;
}

.panel-subtitle,
.panel-copy {
  color: var(--text-dim);
  line-height: 1.7;
}

.panel-subtitle {
  margin: 12px 0 0;
  max-width: 34rem;
}

.panel-tag {
  padding: 8px 12px;
  border: 1px solid var(--line);
  text-transform: uppercase;
  letter-spacing: 0.14em;
  white-space: nowrap;
}

.panel-body {
  display: grid;
  grid-template-columns: 280px minmax(0, 1fr);
  gap: 28px;
  align-items: flex-start;
  margin-top: 28px;
}

.signal-block {
  display: grid;
  gap: 14px;
  justify-items: center;
}

.signal-visual-slot {
  width: 100%;
  min-height: 320px;
  display: grid;
  place-items: center;
}

.signal-visual {
  width: 100%;
  display: grid;
  place-items: center;
}

.signal-radar {
  position: relative;
  width: 220px;
  height: 220px;
  border-radius: 50%;
  border: 2px solid var(--text-strong);
  box-shadow: inset 0 0 0 1px rgba(109, 255, 85, 0.14);
}

.signal-ring,
.signal-cross,
.signal-core {
  position: absolute;
}

.signal-ring {
  inset: 26px;
  border-radius: 50%;
  border: 1px solid var(--line);
}

.signal-ring--inner {
  inset: 56px;
}

.signal-cross--x,
.signal-cross--y {
  background: var(--line-soft);
}

.signal-cross--x {
  top: calc(50% - 1px);
  left: 12px;
  right: 12px;
  height: 2px;
}

.signal-cross--y {
  left: calc(50% - 1px);
  top: 12px;
  bottom: 12px;
  width: 2px;
}

.signal-core {
  inset: 50%;
  width: 88px;
  height: 88px;
  margin: -44px;
  display: grid;
  place-items: center;
  border-radius: 50%;
  border: 2px solid var(--text-strong);
  color: var(--text-strong);
  font-size: 2.2rem;
  font-weight: 700;
  text-transform: uppercase;
  box-shadow: 0 0 24px rgba(102, 255, 68, 0.14);
}

.signal-label {
  padding: 8px 12px;
  border: 1px solid var(--line);
  text-transform: uppercase;
  letter-spacing: 0.16em;
  color: var(--text-strong);
}

.schematic-board {
  width: 240px;
  padding: 16px;
  border: 1px solid var(--line);
  background:
    linear-gradient(180deg, rgba(12, 27, 13, 0.9), rgba(5, 12, 6, 0.98)),
    radial-gradient(circle at top, rgba(109, 255, 85, 0.14), transparent 52%);
  box-shadow: inset 0 0 0 1px var(--surface-glow), 0 0 24px rgba(109, 255, 85, 0.08);
}

.schematic-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12px;
  margin-bottom: 12px;
}

.schematic-chip {
  color: var(--text-dim);
  text-transform: uppercase;
  letter-spacing: 0.16em;
  font-size: 0.72rem;
}

.schematic-badge {
  min-width: 54px;
  height: 38px;
  padding: 0 10px;
  display: grid;
  place-items: center;
  border: 1px solid var(--line);
  color: var(--text-strong);
  font-weight: 700;
  letter-spacing: 0.14em;
}

.schematic-canvas {
  position: relative;
  height: 156px;
  overflow: hidden;
  border: 1px solid var(--line-soft);
  background: linear-gradient(180deg, rgba(13, 29, 14, 0.72), rgba(4, 10, 5, 0.94));
}

.schematic-grid {
  position: absolute;
  inset: 0;
  background:
    linear-gradient(rgba(109, 255, 85, 0.07) 1px, transparent 1px),
    linear-gradient(90deg, rgba(109, 255, 85, 0.07) 1px, transparent 1px);
  background-size: 24px 24px;
  opacity: 0.55;
}

.schematic-sweep {
  position: absolute;
  inset: -20% auto -20% 62%;
  width: 90px;
  transform: rotate(18deg);
  background: linear-gradient(180deg, transparent, rgba(109, 255, 85, 0.12), transparent);
}

.schematic-network,
.schematic-line,
.schematic-node,
.schematic-pulse {
  position: absolute;
}

.schematic-network {
  inset: 0;
}

.schematic-line {
  background: linear-gradient(180deg, rgba(109, 255, 85, 0.18), rgba(109, 255, 85, 0.85), rgba(109, 255, 85, 0.18));
  box-shadow: 0 0 12px rgba(109, 255, 85, 0.16);
}

.schematic-line--vertical {
  left: calc(50% - 1px);
  top: 18px;
  bottom: 18px;
  width: 2px;
}

.schematic-line--horizontal {
  top: calc(50% - 1px);
  left: 18px;
  right: 18px;
  height: 2px;
}

.schematic-node {
  width: 12px;
  height: 12px;
  border-radius: 999px;
  border: 2px solid var(--text-strong);
  background: rgba(4, 16, 6, 0.92);
  box-shadow: 0 0 16px rgba(109, 255, 85, 0.18);
}

.schematic-node--top {
  left: calc(50% - 6px);
  top: 12px;
}

.schematic-node--right {
  right: 12px;
  top: calc(50% - 6px);
}

.schematic-node--bottom {
  left: calc(50% - 6px);
  bottom: 12px;
}

.schematic-node--left {
  left: 12px;
  top: calc(50% - 6px);
}

.schematic-pulse {
  left: 18px;
  right: 18px;
  top: 50%;
  height: 2px;
  transform: translateY(-50%);
  background: linear-gradient(
    90deg,
    transparent 0%,
    transparent 18%,
    rgba(109, 255, 85, 0.45) 18%,
    rgba(109, 255, 85, 0.45) 28%,
    rgba(109, 255, 85, 0.95) 33%,
    rgba(109, 255, 85, 0.18) 38%,
    rgba(109, 255, 85, 0.18) 62%,
    rgba(109, 255, 85, 0.95) 68%,
    rgba(109, 255, 85, 0.45) 74%,
    transparent 82%,
    transparent 100%
  );
}

.schematic-core-panel {
  position: absolute;
  left: 50%;
  top: 50%;
  width: 94px;
  transform: translate(-50%, -50%);
  padding: 10px 8px;
  border: 1px solid var(--line);
  background: rgba(7, 18, 8, 0.92);
  box-shadow: inset 0 0 0 1px var(--surface-glow);
  text-align: center;
}

.schematic-core-panel__eyebrow {
  display: block;
  color: var(--text-dim);
  font-size: 0.62rem;
  letter-spacing: 0.16em;
  text-transform: uppercase;
}

.schematic-core-panel__value {
  display: block;
  margin-top: 6px;
  color: var(--text-strong);
  font-size: 1.4rem;
  font-weight: 700;
  letter-spacing: 0.16em;
}

.schematic-stats {
  display: grid;
  gap: 10px;
  margin-top: 14px;
}

.schematic-stat {
  display: grid;
  gap: 6px;
}

.schematic-stat__meta {
  display: flex;
  justify-content: space-between;
  gap: 10px;
  color: var(--text-dim);
  font-size: 0.74rem;
  text-transform: uppercase;
  letter-spacing: 0.12em;
}

.schematic-stat__track {
  height: 8px;
  border: 1px solid var(--line);
  background: rgba(7, 18, 8, 0.9);
}

.schematic-stat__fill {
  display: block;
  height: 100%;
  background: linear-gradient(90deg, var(--text-strong), rgba(109, 255, 85, 0.35));
}

.panel-copy {
  margin: 0;
}

.panel-bullets {
  margin: 18px 0 0;
  padding-left: 18px;
  color: var(--text);
  line-height: 1.8;
}

.dock {
  display: grid;
  grid-template-columns: 1.15fr 0.85fr;
  gap: 18px;
  margin-top: 18px;
  padding: 18px 20px;
  border-top: none;
}

.signal-cards {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(170px, 1fr));
  gap: 12px;
  margin-top: 12px;
}

.signal-card {
  padding: 14px;
  border: 1px solid var(--line-soft);
  background: rgba(8, 18, 8, 0.72);
}

.signal-card strong {
  display: block;
  color: var(--text-strong);
  font-size: 1.2rem;
  margin-bottom: 6px;
}

.signal-card span {
  color: var(--text-dim);
  line-height: 1.5;
}

.link-pills {
  display: flex;
  flex-wrap: wrap;
  gap: 10px;
  margin-top: 12px;
}

.link-pill {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  padding: 10px 12px;
}

.footer {
  display: flex;
  flex-wrap: wrap;
  gap: 10px;
  padding: 12px 18px;
  border-top: none;
  border-radius: 0 0 18px 18px;
  color: var(--text-dim);
}

.footer-separator {
  color: var(--text-faint);
}

@media (max-width: 960px) {
  .topbar,
  .screen,
  .panel-body,
  .dock {
    display: grid;
    grid-template-columns: 1fr;
  }

  .module-tabs {
    justify-content: flex-start;
  }

  .panel-meta {
    display: grid;
  }

  .signal-visual-slot {
    min-height: auto;
  }
}

@media (max-width: 640px) {
  .shell {
    padding: 14px 10px 24px;
  }

  .topbar,
  .screen,
  .dock,
  .panel-frame,
  .sidebar {
    padding: 14px;
  }

  .schematic-board {
    width: min(100%, 260px);
  }
}

        .page, .page * {
          box-sizing: border-box;
        }

        .page {
          width: 1136px;
          height: 876px;
          color: var(--text);
          font-family: 'IBM Plex Mono', 'SFMono-Regular', Consolas, monospace;
        }

        .page .crt {
          display: none;
        }

        .page .shell {
          max-width: none;
          margin: 0;
          padding: 0;
        }

        .page .tab-button,
        .page .item-button,
        .page .link-pill {
          cursor: default;
        }

        .page .link-pill {
          pointer-events: none;
        }
        ]]></style>
          </defs>

          <rect x="14" y="14" width="1172" height="912" rx="18" fill="url(#bg)" stroke="#57FF3A" stroke-width="2" />
          <rect x="28" y="28" width="1144" height="884" rx="14" fill="#040904" stroke="#1D5724" stroke-width="1.5" />
          <rect x="28" y="28" width="1144" height="884" rx="14" fill="url(#scan)" opacity="0.85" />

          <foreignObject x="32" y="32" width="1136" height="876">
            <div xmlns="http://www.w3.org/1999/xhtml" class="page" style="--bg-start:#030603;--bg-end:#050b05;--panel:rgba(7, 19, 9, 0.94);--line:#2f9134;--line-soft:rgba(76, 180, 80, 0.22);--text:#9dff7b;--text-strong:#6dff55;--text-dim:#62c964;--text-faint:#2f6a33;--shadow:rgba(57, 255, 20, 0.18)">
              <div class="shell">
                <header class="topbar">
                  <div class="brand">
                    <span class="brand-led"></span>
                    <div>
                      <p class="brand-kicker">interactive profile</p>
                      <h1 class="brand-title">retro terminal</h1>
                    </div>
                  </div>

                  <nav class="module-tabs"><div class="tab-button is-active">skills</div><div class="tab-button">builds</div><div class="tab-button">signal</div><div class="tab-button">links</div></nav>
                </header>

                <main class="screen">
                  <aside class="sidebar">
                    <div class="sidebar-header">
                      <p class="eyebrow">module items</p>
                      <h2 class="sidebar-title">skill badges</h2>
                    </div>

                    <div class="item-list">
                <div class="item-button is-active">
                  <span>python</span>
                  <span class="item-button__index">01</span>
                </div>
              
                <div class="item-button">
                  <span>javascript</span>
                  <span class="item-button__index">02</span>
                </div>
              
                <div class="item-button">
                  <span>docker</span>
                  <span class="item-button__index">03</span>
                </div>
              
                <div class="item-button">
                  <span>kubernetes</span>
                  <span class="item-button__index">04</span>
                </div>
              
                <div class="item-button">
                  <span>jenkins</span>
                  <span class="item-button__index">05</span>
                </div>
              </div>
                  </aside>

                  <section class="panel">
                    <div class="panel-frame">
                      <div class="panel-meta">
                        <div>
                          <p class="eyebrow">active module</p>
                          <h2 class="panel-title">Python</h2>
                          <p class="panel-subtitle">Automation, tooling and framework work</p>
                        </div>

                        <div class="panel-tag">primary</div>
                      </div>

                      <div class="panel-body">
                        <div class="signal-block">
                          <div class="signal-visual-slot">
    <div class="signal-visual signal-visual--schematic">
      <div class="schematic-board">
                <div class="schematic-header">
                  <div class="schematic-chip">systems view</div>
                  <div class="schematic-badge">PY</div>
                </div>
        <div class="schematic-canvas">
          <div class="schematic-grid"></div>
          <div class="schematic-sweep"></div>
          <div class="schematic-network">
            <div class="schematic-line schematic-line--vertical"></div>
            <div class="schematic-line schematic-line--horizontal"></div>
            <div class="schematic-node schematic-node--top"></div>
            <div class="schematic-node schematic-node--right"></div>
            <div class="schematic-node schematic-node--bottom"></div>
            <div class="schematic-node schematic-node--left"></div>
            <div class="schematic-pulse"></div>
            <div class="schematic-core-panel">
              <span class="schematic-core-panel__eyebrow">primary</span>
              <span class="schematic-core-panel__value">PY</span>
            </div>
          </div>
        </div>
        <div class="schematic-stats">
                <article class="schematic-stat">
                  <div class="schematic-stat__meta">
                    <span>automation</span>
                    <span>96%</span>
                  </div>
                  <div class="schematic-stat__track">
                    <span class="schematic-stat__fill" style="width:96%"></span>
                  </div>
                </article>
              
                <article class="schematic-stat">
                  <div class="schematic-stat__meta">
                    <span>tooling</span>
                    <span>92%</span>
                  </div>
                  <div class="schematic-stat__track">
                    <span class="schematic-stat__fill" style="width:92%"></span>
                  </div>
                </article>
              
                <article class="schematic-stat">
                  <div class="schematic-stat__meta">
                    <span>delivery</span>
                    <span>88%</span>
                  </div>
                  <div class="schematic-stat__track">
                    <span class="schematic-stat__fill" style="width:88%"></span>
                  </div>
                </article>
              </div>
      </div>
    </div>
          </div>
                          <div class="signal-label">python profile</div>
                        </div>

                        <div class="detail-block">
                          <p class="panel-copy">My default tool for test automation, quality tooling, scripts and the practical glue that keeps delivery moving.</p>
                          <ul class="panel-bullets"><li>Automation frameworks and reusable tooling</li><li>Test infrastructure and diagnostics</li><li>Fast scripting for developer workflows</li></ul>
                        </div>
                      </div>
                    </div>
                  </section>
                </main>

                <section class="dock">
                  <div>
                    <p class="eyebrow">signals</p>
                    <div class="signal-cards">
                <article class="signal-card">
                  <strong>5+</strong>
                  <span>years in QA</span>
                </article>
              
                <article class="signal-card">
                  <strong>3500+</strong>
                  <span>automated tests</span>
                </article>
              
                <article class="signal-card">
                  <strong>20+</strong>
                  <span>pipelines</span>
                </article>
              
                <article class="signal-card">
                  <strong>QA x DevOps</strong>
                  <span>focus</span>
                </article>
              </div>
                  </div>

                  <div>
                    <p class="eyebrow">links</p>
                    <div class="link-pills"><div class="link-pill">GitHub</div><div class="link-pill">Profile README</div></div>
                  </div>
                </section>

                <footer class="footer">
                  <span>private interactive source / public static view</span>
                  <span class="footer-separator">/</span>
                  <span>static view generated from shared data</span>
                </footer>
              </div>
            </div>
          </foreignObject>
        </svg>
